name: home
description: A Sensor Program that extracts information from the sensors on the phone and view Arduino Data
 
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"
  # sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  fab_circular_menu: ^1.0.0 
  #sqflite: ^1.0.0
  #path_provider: ^0.4.1


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  
# Sensorz Git Documentation
# Documentation: https://github.com/itzmeanjan/sensorz/blob/master/README.md
  # sdk: ">=2.0.0-dev.68.0 <3.0.0"
    